MEASURE First_TYPE_Good IS
ENABLED(A.good) -> STATE_REWARD(1.0);

MEASURE First_TYPE_Fail IS
ENABLED(A.fail) -> STATE_REWARD(1.0);

MEASURE First_TYPE_Workload IS
ENABLED(A.workload) -> STATE_REWARD(1.0);

MEASURE Second_TYPE_Good IS
ENABLED(B.good) -> STATE_REWARD(1.0);

MEASURE Second_TYPE_Fail IS
ENABLED(B.fail) -> STATE_REWARD(1.0)
